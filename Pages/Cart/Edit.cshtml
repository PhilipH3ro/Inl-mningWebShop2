@page "{id:min(0)}"
@model DC2.UI.Pages.Cart.EditModel
@{
    if (User.Identity.IsAuthenticated)
    {
        Layout = "_LayoutLogedIn";
    }
}

<center>
    <h1>Edit Current Product</h1>
    <br>
</center>

@*<div>@Model.CurrentUser.FirstName</div>
    <form asp-page-handler="Save">
    <input type="hidden" asp-for="CurrentUser.Id" value="@Model.CurrentUser.Id" />
    <input type="text" asp-for="CurrentUser.FirstName" value="@Model.CurrentUser.FirstName" />
    <input type="text" asp-for="CurrentUser.LastName" value="@Model.CurrentUser.LastName" />
    <input type="email" asp-for="CurrentUser.Email" value="@Model.CurrentUser.Email" />
    <input type="text" asp-for="CurrentUser.PhoneNumber" value="@Model.CurrentUser.PhoneNumber" />
    <input type="text" asp-for="CurrentUser.Secret" value="@Model.CurrentUser.Secret" />

    <button type="submit" class="btn btn-primary">Submit</button>

    </form>*@

<form asp-page-handler="Save">
    <input type="hidden" class="form-control" asp-for="CurrentCart.CartId" value="@Model.CurrentCart.CartId">
    <div class="form-group">
        <label for="productid">ProductId</label>
        <input type="text" class="form-control" asp-for="CurrentCart.ProductId" value="@Model.CurrentCart.ProductId" placeholder="Enter ProductId">
    </div>                                                     
    <div class="form-group">                                   
        <label for="quantity">Quantity</label>           
        <input type="text" class="form-control" asp-for="CurrentCart.Quantity" value="@Model.CurrentCart.Quantity" placeholder="Enter Quantity">
    </div>                                                   

    @*<div class="form-check">
        <input type="checkbox" class="form-check-input" id="exampleCheck1">
        <label class="form-check-label" for="exampleCheck1">I accept all cookies on this website</label>
        </div>*@
    <button type="submit" class="btn btn-primary">Submit</button>
    <a asp-page="./Index" class="btn btn-primary">Back</a>
</form>
@*<h1>TEST</h1>
    <h6>ID: @Model.CurrentUser.Id</h6>
    <h6>Namn: @Model.CurrentUser.FirstName @Model.CurrentUser.LastName</h6>
    <h6>Email: @Model.CurrentUser.Email</h6>
    <h6>PhoneNumber: @Model.CurrentUser.PhoneNumber</h6>
    <h6>Secret: @Model.CurrentUser.Secret</h6>*@